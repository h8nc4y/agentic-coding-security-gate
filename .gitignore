.DS_Store
Thumbs.db
.env
.env.*
*.log
*.tmp
.test-tmp/
node_modules/
__pycache__/
# Agent-local scratch dirs: ignored here so they are not committed, and kept in
# sync with the scanner's work-tree fallback exclusions (scan-private-markers.ps1).
.claude/
.codex/
